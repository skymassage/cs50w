def square(x):
    return x * x

# The "assert" command is followed by some expression that should be True. 
# If the expression is True, nothing will happen, and if it is False, an exception (AssertionError) will be thrown. 
assert square(10) == 100
assert square(10) == 101